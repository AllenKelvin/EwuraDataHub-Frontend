## Packages
framer-motion | For smooth page transitions and micro-interactions
lucide-react | Beautiful icons for the interface

## Notes
- Tailwind Config needs to extend fontFamily for 'Outfit' and 'Inter'
- API handles auth via session cookies (credentials: "include")
- Backend routes are defined in @shared/routes, use api object for URLs
